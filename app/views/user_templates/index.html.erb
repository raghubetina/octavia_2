<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All users
    </h1>

    <a href="/users/new" class="btn btn-block btn-outline-secondary">
      Add a new user
    </a>
  </div>
</div>

<!-- Ransack Code start -->

<!-- Ransack Code end -->

<!-- Location Map Code start -->

<!-- Location Map Code end -->

<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          Email
        </th>

        <th>
          Password
        </th>

        <th>
          Username
        </th>

        <th>
          Curl pattern
        </th>

        <th>
          Porosity
        </th>

        <th>
          Density
        </th>

        <th>
          Age group
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @users.each do |user| %>
      <tr>
        <!-- Display user.id start -->
        <td>
          <%= user.id %>
        </td>
        <!-- Display user.id end -->

        <!-- Display user.email start -->
        <td>
          <%= user.email %>
        </td>
        <!-- Display user.email end -->

        <!-- Display user.password start -->
        <td>
          <%= user.password %>
        </td>
        <!-- Display user.password end -->

        <!-- Display user.username start -->
        <td>
          <%= user.username %>
        </td>
        <!-- Display user.username end -->

        <!-- Display user.curl_pattern_id start -->
        <td>
          <% if user.curl_pattern.present? %>
            <a href="/curl_patterns/<%= user.curl_pattern.id %>">
              <%= user.curl_pattern.number %>
            </a>
          <% end %>
        </td>
        <!-- Display user.curl_pattern_id end -->

        <!-- Display user.porosity_id start -->
        <td>
          <% if user.porosity.present? %>
            <a href="/porosities/<%= user.porosity.id %>">
              <%= user.porosity.level %>
            </a>
          <% end %>
        </td>
        <!-- Display user.porosity_id end -->

        <!-- Display user.density_id start -->
        <td>
          <% if user.density.present? %>
            <a href="/densities/<%= user.density.id %>">
              <%= user.density.level %>
            </a>
          <% end %>
        </td>
        <!-- Display user.density_id end -->

        <!-- Display user.age_group_id start -->
        <td>
          <% if user.age_group.present? %>
            <a href="/age_groups/<%= user.age_group.id %>">
              <%= user.age_group.range %>
            </a>
          <% end %>
        </td>
        <!-- Display user.age_group_id end -->

        <!-- Display user.created_at start -->
        <td>
          <%= time_ago_in_words(user.created_at) %> ago
        </td>
        <!-- Display user.created_at end -->

        <!-- Display user.updated_at start -->
        <td>
          <%= time_ago_in_words(user.updated_at) %> ago
        </td>
        <!-- Display user.updated_at end -->

        <td>
          <a href="/users/<%= user.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>

    <div class="row mb-3">
      <div class="col-md-12">
        <%= paginate @users, theme: "twitter-bootstrap-4" %>
      </div>
    </div>

  </div>
</div>
