<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All curl patterns
    </h1>

    <a href="/curl_patterns/new" class="btn btn-block btn-outline-secondary">
      Add a new curl pattern
    </a>
  </div>
</div>

<!-- Ransack Code start -->
<div class="row">
  <div class="col-md-12 mb-3">
    <%= render partial: "curl_pattern_templates/curl_patterns_search_form" %>
  </div>
</div>
<!-- Ransack Code end -->

<!-- Location Map Code start -->

<!-- Location Map Code end -->

<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          Number
        </th>

        <th>
          Letter
        </th>

        <th>
          Description
        </th>

        <th>
          Position
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @curl_patterns.each do |curl_pattern| %>
      <tr>
        <!-- Display curl_pattern.id start -->
        <td>
          <%= curl_pattern.id %>
        </td>
        <!-- Display curl_pattern.id end -->

        <!-- Display curl_pattern.number start -->
        <td>
          <%= curl_pattern.number %>
        </td>
        <!-- Display curl_pattern.number end -->

        <!-- Display curl_pattern.letter start -->
        <td>
          <%= curl_pattern.letter %>
        </td>
        <!-- Display curl_pattern.letter end -->

        <!-- Display curl_pattern.description start -->
        <td>
          <%= curl_pattern.description %>
        </td>
        <!-- Display curl_pattern.description end -->

        <!-- Display curl_pattern.position start -->
        <td>
          <%= curl_pattern.position %>
        </td>
        <!-- Display curl_pattern.position end -->

        <!-- Display curl_pattern.created_at start -->
        <td>
          <%= time_ago_in_words(curl_pattern.created_at) %> ago
        </td>
        <!-- Display curl_pattern.created_at end -->

        <!-- Display curl_pattern.updated_at start -->
        <td>
          <%= time_ago_in_words(curl_pattern.updated_at) %> ago
        </td>
        <!-- Display curl_pattern.updated_at end -->

        <td>
          <a href="/curl_patterns/<%= curl_pattern.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>

    <div class="row mb-3">
      <div class="col-md-12">
        <%= paginate @curl_patterns, theme: "twitter-bootstrap-4" %>
      </div>
    </div>

  </div>
</div>
